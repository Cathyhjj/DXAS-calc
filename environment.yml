# Legacy Notebook environment. The web application requires Python 3.9+;
# use requirements.txt or pyproject.toml for the web service.
name: dxascalc
channels:
  - defaults
  - conda-forge 
dependencies:
  - python=3.8
  - numpy
  - matplotlib
  - scipy
  - pip:
    - pip
    - xraylib
    - dabax
    - pandas
    - matplotlib
    - ipywidgets
    - xraylib
    - xoppylib
    - dabax
